package com.example.ui.theme

import androidx.compose.ui.graphics.Color

// --- Geometric Balance Theme Palette ---

// Brand Colors
val GeoPrimary = Color(0xFF386B40)          // Balanced brand green
val GeoPrimaryVariant = Color(0xFF1B5E20)   // Dark emerald highlight accent
val GeoStreakOrange = Color(0xFFFF9800)     // Flame orange streak accent

// Light Theme Colors
val LightBackground = Color(0xFFF7FAF7)     // Soft cool off-white-green canvas
val LightSurface = Color(0xFFFFFFFF)        // Pure white card surfaces
val GeoMutedText = Color(0xFF424940)        // Secondary slate-green body
val GeoCaptions = Color(0xFF727970)         // Soft captioning text
val GeoLightContainer = Color(0xFFE8F5E9)   // Sage highlights background
val GeoLightContainerBorder = Color(0xFFC8E6C9) // Sage leaf card border
val GeoMutedBorder = Color(0xFFDDE5DB)      // Soft gray-sage borders
val SlateDark = Color(0xFF1A1C19)           // Deep charcoal high-contrast text

// Dark Theme Colors
val DarkCanvas = Color(0xFF111E14)          // Deep forest-slate background canvas
val DarkPremiumSurface = Color(0xFF1C2920)  // Dark sage elevated surfaces
val DarkTextLight = Color(0xFFE1E4DF)       // Bright sage-white text
val DarkTextMuted = Color(0xFF929990)       // Muted gray-green secondary
val MintAccent = Color(0xFF8CD795)          // Highly-visible mint primary in dark
val LimeSage = Color(0xFFA5D6A7)            // Soft lime-green secondary in dark

